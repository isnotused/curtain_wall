[project]
name = "facade-unit-generator"
version = "0.1.0"
description = "Dataset utilities for the parametric facade unit generation & verification console"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.4",
    "pandas>=2.3.3",
]
